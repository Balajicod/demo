const path = require("path");

 const rootDirect = path.dirname(require.main.filename)

module.exports = rootDirect;
 const rootDir = path.dirname(require.main.filename)

module.exports = rootDir;
 
 
